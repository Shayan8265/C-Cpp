/*
CH-230-A
a1 p1.1[c]
Shayan Ali Khan
skhan01@jacobs-university.de
*/
/*explanation; in the problem the values were in integer form but the formating specification
was a double, so it needed decimal values to give the decimal result*/
#include <stdio.h>

int main() {
 double result;  /* The result of our calculation */
 result = (3.0 + 1.0)/5.0;
  printf("The value of 4/5 is %1f\n", result);
return 0;
}
